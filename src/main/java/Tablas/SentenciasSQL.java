/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package Tablas;

/**
 *
 * @author sjgar
 */
public interface SentenciasSQL {
    
    
    /*
    
    SELECT c.nombre, c.id_plan, p.nombre FROM cliente AS c INNER JOIN plan AS p ON c.id_plan = p.id_plan;
    SELECT e.nombre, e.id_gym FROM empleados AS e INNER JOIN gimnasio AS g ON e.id_gym = g.id_gym;
    
    */
    
}
